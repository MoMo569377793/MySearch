# Paper Monitor

本项目用于本地监控指定领域论文，支持 `arXiv`、`DBLP` 和 `Google Scholar Alerts` 邮件解析，支持本地 PDF 下载与全文抽取，并支持两种 LLM 接入方式：

- `openai_compatible`：兼容 `chat/completions`
- `openai_responses`：OpenAI 原生 `Responses API`

完整中文手册见 [使用手册.md](使用手册.md)。
